# Dufs RAM Server 0.51.10

- 升级到不可变 Foundation 0.8.1 Rust/npm 依赖与完整 source revision。
- 删除已移除的 `WorkspaceConfig.layout` 配置，继续使用 Foundation 默认全宽内容。
- 重新生成平台 Web 产物，确认不包含任何实例侧栏 CSS 或 DOM 能力。
- 浏览器回归与 Foundation 的无确认框语言直接切换行为保持一致。
