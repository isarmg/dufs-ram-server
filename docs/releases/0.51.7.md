# 0.51.7

- 大目录性能工作流在 Rust 基准前安装锁定的 Web 依赖并构建嵌入资源，消除 `build.rs` 缺少 `web/dist` 的确定性失败。
- 正式包 E2E 导出已经完成签名、校验、解包和真实运行验证的同一候选产物。
- GitHub 发布工作流等待精确 SHA 的正式包测试，从该运行下载候选包、复验签名与来源，再直接发布其中的同一二进制，不再重建另一个未经过该 E2E 的二进制。
- 浏览器矩阵及正式包失败时保留有限期 trace、report 与日志，成功运行不上传调试数据。
