# dufs-ram-server 0.51.2

本版本修复 Web 首屏会先显示恢复会话提示再进入登录页的问题，并在 Maple 字体加载完成前保持不透明启动占位，避免默认字体闪现。

管理页面只保留右上角人物图标进入的当前管理员信息设置；移除独立的平台管理员列表/管理页面。

文件页将根目录、上传、新建和搜索统一放入顶部菜单栏，与刷新、语言、主题、注销等控件保持同一菜单容器；菜单内容允许在窄屏换行，但不会回落到独立工具栏。

Rust 与 Web 依赖已固定 Foundation Server 0.7.3，发布前完成 Chromium/Firefox 回归和发行身份核验。
