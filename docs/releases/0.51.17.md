# dufs-ram-server 0.51.17

- Web 上传在创建任务前验证完整目标路径及字节长度，包括多层目录和文件名；无效目标不会产生后台任务。
- 文档明确当前下载、清理与持久化状态行为。数据库状态身份仍是 `0.51.0`；使用 `sarmg-upgrade support --json` 核对离线维护能力。
- Rust 与 Web 共享机制固定 Foundation Server `0.9.2` 的完整 Git revision 及同版 Web Release 资产。

GitHub 标签工作流构建并核对 Linux 二进制与 SHA-256。需要独立签名、SBOM 和许可证清单时使用仓库的正式打包流程。
