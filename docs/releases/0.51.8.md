# 0.51.8

- 将正式发布自测从主发布脚本拆分为仅在 `--self-test` 时加载的独立模块，缩小生产发布路径的职责范围。
- `check.sh` 与 `package-release.sh` 共用同一个 Node.js 精确版本校验实现，删除重复的工具链解析逻辑。
- 完整 Rust、覆盖率、部署、release 运行时以及 Chromium/Firefox 浏览器门禁保持不变并全部通过。
