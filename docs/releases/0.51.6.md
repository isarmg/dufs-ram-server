# Dufs RAM Server 0.51.6

修复正式发行端到端门禁中残留的浏览器布局竞态：顶部操作栏 containment 现在只在 `expect.poll` 内对同一帧 DOMRect 做原子比较，不再于等待成功后重新采样两次可能跨响应式重排帧的边界框。

`v0.51.4` 与 `v0.51.5` 保留为失败发行尝试的审计标签且不发布可安装制品；本版必须通过完整只读 CI、依赖审计和正式签名包端到端验证后才自动发布。
