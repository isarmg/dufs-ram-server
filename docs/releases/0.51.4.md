# Dufs RAM Server 0.51.4

本版本升级到 Foundation Server 0.7.6，并统一管理 Web 的垂直内容间距。顶部菜单栏到文件工具栏在桌面、平板和窄屏换行布局下均保持 16px；根目录、上传、新建、搜索和刷新继续使用同一全局菜单栏逻辑。

浏览器回归覆盖 1280px、768px 与 320px 宽度，确认菜单换行不会改变内容起始距离，也不会产生横向溢出。
