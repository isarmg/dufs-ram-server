# Dufs RAM Server 0.51.5

修复正式发行端到端门禁中的浏览器布局时序：Foundation 顶部布局断言会先等待嵌入字体和响应式重排稳定，避免 Firefox/Chromium 在字体替换或恢复桌面视口期间读取过渡尺寸并产生 flaky 结果。

`v0.51.4` 与 `v0.51.5` 均保留为失败发行尝试的审计标签，不发布可安装制品。`v0.51.5` 的首次稳定等待已生效，但随后又以未同步的第二次边界框采样重复断言，正式门禁据此正确停止发布。
