# dufs-ram-server 0.51.16

标签发行会先校验注释标签和完整源码 SHA，再在只读任务中审计锁定依赖、构建 Linux release 二进制、核对完整版本与动态库、生成并校验 SHA-256。拥有写权限的发布任务仅消费同次构建的精确产物并复核后创建公开 Release。

完整签名包 E2E 可按需手动执行，不再阻塞便捷二进制 Release。主分支 CI 仍执行 Rust、Web、浏览器、覆盖率和部署回归。GitHub Release 提供二进制及校验和；需要独立签名、SBOM 与许可证清单时使用本地正式打包流程。
