# Server 仓库

当前目录及 GitHub 仓库为 `dufs-ram-server`：https://github.com/isarmg/dufs-ram-server 。
服务端及内置 React Web 同属本仓库。浏览器页面不是独立安装客户端，当前没有需要拆出的原生客户端，
因此不创建空的 `dufs-ram-client` 仓库。历史提交和发行标签保留，产品可执行文件及运行数据路径不因仓库改名而改写。
